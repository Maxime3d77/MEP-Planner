# Health Center

The Health Center reports an overall score, database and API state, scheduler status, optional integration configuration, disk usage, database size, backup count, uptime and active alerts. A disabled optional integration is informational and does not mean the application is unhealthy.
