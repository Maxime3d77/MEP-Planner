# OpenID Connect

Open **Administration → SSO / OIDC** and configure the discovery URL, client credentials, claims and group mappings. Register the redirect URL displayed by MEP Planner in the identity provider. Test with a non-administrator account before enforcing SSO.
