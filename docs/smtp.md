# SMTP

Open **Administration → SMTP**, enable SMTP and configure the server, port, security mode, credentials, sender and default recipients. Use the built-in test button before enabling automatic communications. STARTTLS on port 587 is the usual recommended configuration.
