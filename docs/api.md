# API

The FastAPI backend exposes application, settings, authentication, Redmine synchronization, communications, Health Center and Backup Center endpoints. Interactive OpenAPI documentation is available at `/docs` when it is not disabled by the deployment proxy. Administrative endpoints require an authenticated administrator token.
