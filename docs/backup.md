# Backup Center

The Backup Center can create, download, import, restore and delete ZIP backups. A manifest records the MEP Planner version and included data. Restore operations automatically create a safety backup first. Store exported backups outside the application host and regularly test restoration on a non-production instance.
