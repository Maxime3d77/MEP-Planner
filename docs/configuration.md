# Configuration

Copy `.env.example` to `.env` and configure the public URL, timezone, Redmine connection and initial administrator settings. Secrets must never be committed to Git. Runtime settings saved from the interface are stored in the persistent database and survive updates.
